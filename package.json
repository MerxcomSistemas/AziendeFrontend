{
  "name": "aziende-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:host": "cd \"Starterkit - ts\" && npm run dev",
    "dev:editor": "cd AziendePlatformEditor && npm run dev",
    "build": "node scripts/build.js",
    "build:host": "cd \"Starterkit - ts\" && npm run build",
    "build:editor": "cd AziendePlatformEditor && npm run build",
    "preview": "node scripts/preview.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
